# Leave empty to use the Vite dev proxy (recommended for local development).
# VITE_API_BASE_URL=

# Proxy target when using npm run dev (defaults to harbour-cloud on port 8080).
# VITE_PROXY_TARGET=http://localhost:8080
